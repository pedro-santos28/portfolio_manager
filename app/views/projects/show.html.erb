<div class="d-flex flex-column overflow-y-auto">
  <h1 class="text-center fs-3">Informações do projeto</h1>
  <%= render partial: "show", locals: {project: @project}%>
  <div class="d-flex w-100 justify-content-center gap-4">
    <%= link_to "Voltar", projects_path, class: "btn btn-secondary " %>
    <%= link_to "Editar", edit_project_path(@project['id']), class: "btn btn-warning " %>
  </div>
</div>
