<div class="d-flex flex-column gap-8">
  <h1 class="text-primary">Perfil</h1>
  <%= link_to "Editar", edit_profile_path(@profile['id']),  class: "fs-4 position-absolute end-0 btn btn-warning" %>
  <div class="d-flex flex-column gap-4 align-items-center border">
    <div class="position-relative d-flex align-items-center justify-content-center w-100">
      <h1 class="text-danger">
        <%= @profile['name']  %>
      </h1>
    </div>
    <p class="fs-3">
      <%= @profile['description'] %>
    </p>
  </div>
  <div class="d-flex flex-column gap-4 justify-content-center">
    <div class="h-full d-flex flex-column justify-content-around">
      <% @profile['traits'].each do |trait| %>
        <span class="fs-4">
          <%= trait['description']%>.
        </span>
      <% end %>
    </div>
    <div class="">
      <%= 
        image_tag(@profile['image'], size:"250x250", class: "rounded") 
      %>
    </div>
  </div>
</div>
